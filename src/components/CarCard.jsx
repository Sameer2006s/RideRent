import { ArrowUpRight, Users, Fuel, Cog, CalendarCheck } from 'lucide-react';

export default function CarCard({ car, index, onDetails, onBook }) {
  return <article className="car-card" style={{ '--delay': `${index * 45}ms` }}><div className="car-image-wrap"><img src={car.image} alt={car.name === 'Maruti Brezza' ? 'Maruti Suzuki Brezza' : car.name} loading="lazy" /><span className={`availability ${car.available ? '' : 'unavailable'}`}><i />{car.available ? 'Available now' : 'Coming soon'}</span><span className="card-index">{String(index + 1).padStart(2, '0')}</span></div><div className="car-card-body"><div className="car-card-top"><div><span className="car-category">{car.category}</span><h3>{car.name}</h3></div><div className="card-price">₹{car.price.toLocaleString('en-IN')}<small>/ day</small></div></div><div className="car-specs"><span><Users size={14} /> {car.seats} seats</span><span><Fuel size={14} /> {car.fuel}</span><span><Cog size={14} /> {car.transmission}</span></div><div className="card-actions"><button className="button button-outline" onClick={() => onDetails(car)}>View details <ArrowUpRight size={15} /></button><button className="button button-accent" onClick={() => onBook(car)} disabled={!car.available}><CalendarCheck size={15} /> Rent now</button></div></div></article>;
}
